@model IEnumerable<ModelCL.Tema>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TemaNombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TemaDescripcion)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TemaNombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TemaDescripcion)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TemaId }) |
            @Html.ActionLink("Details", "Details", new { id=item.TemaId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TemaId })
        </td>
    </tr>
}

</table>
